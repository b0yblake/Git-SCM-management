// Public surface of the renderer ports feature (ARCHITECTURE.md §4).
// The app shell mounts the host; nothing else needs anything from here.
export { PortsModalHost } from './components/PortsModalHost'
